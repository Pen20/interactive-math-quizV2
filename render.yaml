services:
  - type: web
    name: interactive-math-quiz
    env: node
    rootDir: server
    plan: free
    autoDeploy: true

    buildCommand: npm ci
    startCommand: node index.js
    healthCheckPath: /healthz

    # Only declare the secret you actually need.
    envVars:
      - key: OPENAI_API_KEY
        sync: false # you’ll paste the value in the Render UI
